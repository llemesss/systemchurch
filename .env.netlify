# Configurações para deploy no Netlify
NODE_ENV=production

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@ep-example.us-east-2.aws.neon.tech/neondb?sslmode=require

# JWT Configuration
JWT_SECRET=igreja_jwt_secret_2024_super_secure_key_netlify_production

# CORS Configuration
ALLOWED_ORIGINS=https://idpb.netlify.app,http://localhost:5173,http://localhost:5174

# Frontend Environment Variables (para o build)
VITE_API_URL=https://idpb.netlify.app/.netlify/functions/api